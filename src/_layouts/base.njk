<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} – {% endif %}{{ meta.siteName }}</title>
  <link rel="stylesheet" href="{{ meta.url }}/css/styles.css" />
</head>
<body>
  {% block main %}
    <main>
      <h1>{{ title }}</h1>
      {{ content | safe }}
    </main>
  {% endblock %}
</body>
</html>
